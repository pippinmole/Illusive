@page
@using Westwind.AspNetCore.Markdown
@using Humanizer
@using global::Illusive.Illusive.Database.Interfaces
@inject IAccountService AccountService
@model ForumPost
@{
    ViewData["Title"] = this.Model.ForumPostData.Title;
}

<!DOCTYPE html>

<html>
<head>
    <title>@this.Model.ForumPostData.Title</title>
    
    <style type="text/css">
        
        .commentField {
            width: 390px; 
            border: 1px solid #999999; 
            padding: 5px;
            margin: 10px 0;
        }
        
        .forumTitle {
            margin-top: 25px;
        }
        
        .forumContent {
            background-color: #f1f1f1;
            border-bottom-left-radius: 7px;
            border-bottom-right-radius: 7px;
            padding: 15px;
            box-shadow: 0 1rem 1rem rgba(0, 0, 0, .1);
            margin-top: 30px;
            margin-bottom: 30px;
            word-wrap: break-word;
        }
        
    </style>
</head>
<body>
<div>
    <span style="display: block ruby">
        <h3 class="forumTitle">@this.Model.ForumPostData.Title</h3>
        @foreach ( var tag in this.Model.ForumPostData.Tags.Split(',') ) {
            <div class="forumTag noselect">@tag</div>
        }
    </span>
    <div class="forumContent">
        <span style="font-style: italic; font-size: 0.6rem; display: flex; margin-bottom: 10px">Created @this.Model.ForumPostData.TimeCreated.Date.ToString("MMMM dd, yyyy") (@DateTime.Now.Subtract(this.Model.ForumPostData.TimeCreated.Date).Humanize() ago)</span>
        @{
            var isMarkup = this.Model.ForumPostData.IsMarkdown;
            if ( isMarkup ) {
                <p>
                    @Markdown.ParseHtmlString(this.Model.ForumPostData.Content)
                </p>
            } else {
                <p>
                    @this.Model.ForumPostData.Content
                </p>
            }
        }
    </div>

    <h4>Comments</h4>

    @if ( !this.User.Identity.IsAuthenticated ) {
        <div class=""><a href="/Login">Login</a> or <a href="/Signup">Sign up</a> to comment on a post!</div>
    }

    @{
        var replies = this.Model.ForumPostData.Replies;
        foreach ( var reply in replies ) {
            <div style="display: flex">
                @{
                    var replyOwner = AccountService.GetAccountWhere(x => x.Id == reply.AuthorId);
                }
                <img src="@replyOwner?.ProfilePicture" alt="" style="height: 15px; width:15px; align-self: center; margin-right: 5px" />
                @replyOwner?.AccountName: @reply.Content
                <span style="margin-left: 10px; font-style: italic; font-size: 0.6rem; align-self: center">(@reply.TimeCreated.Humanize())</span>
            </div>
        }
    }

    @{
        if ( this.User.Identity.IsAuthenticated ) {
            <form method="post">
                Reply: <input class="commentField" asp-route-id="@this.Model.ForumPostData.Id" asp-for="ForumReply.Content" value=""/><br/>
                <button type="submit">Comment on this</button>
            </form>
        }
    }
</div>
</body>
</html>